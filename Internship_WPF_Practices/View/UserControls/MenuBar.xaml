<UserControl x:Class="Internship_WPF_Practices.View.UserControls.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Internship_WPF_Practices.View.UserControls"
             mc:Ignorable="d" 
             Height="70" d:DesignWidth="800">

    <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <Menu>
                <MenuItem Header="File" FontSize="16">
                    <MenuItem Header="New"/>
                    <MenuItem Header="Open"/>
                    <MenuItem Header="Exit"/>
                </MenuItem>
                <MenuItem Header="Edit" FontSize="16"/>
            </Menu>
            
            <!-- 1. Yöntem: Alt satıra yeni bir Grid tanımlama -->
       <!--     <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <Button Width="50" Margin="5"/>
            <TextBox Grid.Column="1" Width="150" Margin="5"/>
            </Grid>  -->
        
        <!-- 2. Yöntem: Stack Panel ile-->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Width="50" Margin="5"/>
            <TextBox Grid.Column="1" Width="150" Margin="5"/>
        </StackPanel>
        
        
        </Grid>


   
</UserControl>
